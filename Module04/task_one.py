# Importing from MyModule file the functions defined previosly
from MyModule import get_a_list_of_numbers, find_max, find_min

# Getting the list of numbers
numbers_list = get_a_list_of_numbers()

# Finding the minimum value of the list entered
min_in_list = find_min(numbers_list)

# Finding the maximum value of the list entered
max_in_list = find_max(numbers_list)
